function [ Ifv ] = to_visualize_TF( If )
% to_visualize_TF(If) qui prend en paramètres une matrice If contenant la
% transformée de Fourier d'une image, et renvoie le module centré de la
% transformée 

Ifv = abs(If);

end

